#  Finish the solution so that it returns the sum of all the multiples of 3 or 5 below the number passed in. Courtesy of ProjectEuler.net

def solution(number):
    return sum(i for i in range(number) if i % 3 == 0 or i % 5 == 0)
print(solution(int(input())))
